.page-header
  展示

%p#notice= notice

%p
  %b Name:
  = @tag.name
%p
  %b Sort:
  = @tag.sort

= link_to '修改', edit_tag_path(@tag), :class => 'btn btn-mini btn-primary'
\|
= link_to '返回', tags_path, :class => 'btn btn-mini'
